version: "3.9"
services:
  redis:
    image: redis:alpine
    command: redis-server  --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
  rabbitmq:
    image: rabbitmq:3-management-alpine
    ports:
      - 5672:5672
      - 15672:15672
